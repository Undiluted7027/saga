{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saga.dev/schema/evidence-card-0.1.json",
  "title": "Saga Evidence Card",
  "type": "object",
  "required": ["schema_version", "target", "claims", "boundaries", "diagnostics"],
  "properties": {
    "schema_version": { "const": "0.1" },
    "target": { "$ref": "#/$defs/target" },
    "claims": { "type": "array", "items": { "$ref": "#/$defs/claim" } },
    "boundaries": { "type": "array", "items": { "$ref": "#/$defs/boundary" } },
    "diagnostics": { "type": "array", "items": { "$ref": "#/$defs/diagnostic" } }
  },
  "$defs": {
    "span": { "type": "object", "required": ["path", "start_line", "start_column", "end_line", "end_column"], "properties": { "path": { "type": "string" }, "start_line": { "type": "integer", "minimum": 1 }, "start_column": { "type": "integer", "minimum": 0 }, "end_line": { "type": "integer", "minimum": 1 }, "end_column": { "type": "integer", "minimum": 0 } } },
    "target": { "type": "object", "required": ["path", "qualified_name", "name", "signature", "source_span"], "properties": { "path": { "type": "string" }, "qualified_name": { "type": "string" }, "name": { "type": "string" }, "signature": { "type": "string" }, "source_span": { "$ref": "#/$defs/span" } } },
    "claim": { "type": "object", "required": ["id", "kind", "statement", "evidence", "source_spans", "assumptions", "boundary_ids"], "properties": { "id": { "type": "string" }, "kind": { "enum": ["rejected_input", "explicit_exception", "attempted_write", "known_effect", "return_dependency", "test_observation"] }, "statement": { "type": "object", "required": ["text"], "properties": { "text": { "type": "string" } } }, "evidence": { "type": "object", "required": ["method", "evidence_class", "detail"], "properties": { "method": { "type": "string" }, "evidence_class": { "enum": ["derived", "observed"] }, "detail": { "type": "object" } } }, "source_spans": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/span" } }, "assumptions": { "type": "array", "items": { "type": "object", "required": ["text"], "properties": { "text": { "type": "string" } } } }, "boundary_ids": { "type": "array", "items": { "type": "string" } } } },
    "boundary": { "type": "object", "required": ["id", "kind", "target", "reason", "source_span"], "properties": { "id": { "type": "string" }, "kind": { "enum": ["unresolved_call", "dynamic_dispatch", "assignment_hooks", "unsupported_semantics"] }, "target": { "type": "object", "required": ["text"], "properties": { "text": { "type": "string" } } }, "reason": { "type": "string" }, "source_span": { "$ref": "#/$defs/span" } } },
    "diagnostic": { "type": "object", "required": ["kind", "message"], "properties": { "kind": { "type": "string" }, "message": { "type": "string" } } }
  }
}
